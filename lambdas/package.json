{
  "name": "lambdas",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf *lambda/**/*.js *lambda/**/*.zip",
    "build": "tsc && npm run zip:all",
    "zip:all": "npm run zip:events;",
    "zip:events": "cd events-lambda && zip events.zip *.js && cd .."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/aws-lambda": "^8.10.101",
    "@types/jest": "^28.1.4",
    "@types/node": "^18.0.0",
    "aws-sdk": "^2.1166.0",
    "typescript": "^4.7.4"
  }
}
